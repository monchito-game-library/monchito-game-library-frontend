/** Tamaños del retro-spinner Terminal Collector. */
export type LibSpinnerSize = 'inline' | 'sm' | 'md' | 'lg';

/** Variantes de glyph del retro-spinner Terminal Collector. */
export type LibSpinnerVariant = 'bars' | 'dots' | 'blocks';
