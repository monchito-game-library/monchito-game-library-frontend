/** Minimum zoom scale allowed in the cover position / crop interaction UI. */
export const MIN_SCALE: number = 1;

/** Maximum zoom scale allowed in the cover position / crop interaction UI. */
export const MAX_SCALE: number = 4;
