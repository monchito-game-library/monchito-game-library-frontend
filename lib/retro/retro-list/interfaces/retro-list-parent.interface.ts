/**
 * Contrato del padre que retro-list-item requiere para funcionar.
 * Implementado por RetroListComponent vía InjectionToken.
 * En el futuro puede exponer configuración compartida (densidad, padding por defecto).
 */
export interface RetroListParent {}
