/** Variantes visuales del botón lib-button Terminal Collector. */
export type LibButtonVariant = 'primary' | 'ghost' | 'danger' | 'success';

/** Tipos de botón HTML estándar para lib-button. */
export type LibButtonType = 'button' | 'submit' | 'reset';

/** Variantes visuales de lib-card. */
export type LibCardVariant = 'default' | 'accent' | 'muted';

/** Colores semánticos disponibles para lib-chip. */
export type LibChipColor = 'primary' | 'green' | 'amber' | 'rose' | 'blue' | 'neutral';

/** Variantes visuales del lib-badge Terminal Collector. */
export type LibBadgeVariant = 'primary' | 'success' | 'warning' | 'danger' | 'info' | 'neutral';

/** Variantes visuales del lib-icon-button Terminal Collector. */
export type LibIconButtonVariant = 'primary' | 'ghost' | 'danger';

/** Tamaños del lib-icon-button: sm (32) matSuffix, md (40) topbars, lg (44) standalone. */
export type LibIconButtonSize = 'sm' | 'md' | 'lg';

/** Tamaños del lib-spinner Terminal Collector. */
export type LibSpinnerSize = 'inline' | 'sm' | 'md' | 'lg';

/** Variantes de glyph del lib-spinner Terminal Collector. */
export type LibSpinnerVariant = 'bars' | 'dots' | 'blocks';

/** Alias semántico de forma del lib-skeleton (no altera la geometría rectangular). */
export type LibSkeletonShape = 'line' | 'square' | 'block';

/** Tamaños del lib-checkbox: sm (0.875rem) o md (1rem, default). */
export type LibCheckboxSize = 'sm' | 'md';
