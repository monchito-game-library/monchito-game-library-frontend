/** Colores semánticos disponibles para retro-chip. */
export type RetroChipColor = 'primary' | 'green' | 'amber' | 'rose' | 'blue' | 'neutral';

/** Tamaños del retro-chip: sm (h=1rem, fs=10px, denso), md (h=1.25rem, fs=11px, default), lg (h=1.5rem, fs=12px, destacado). */
export type RetroChipSize = 'sm' | 'md' | 'lg';
