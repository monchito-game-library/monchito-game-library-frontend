/** Variantes visuales del botón retro-button Terminal Collector. */
export type LibButtonVariant = 'primary' | 'ghost' | 'danger' | 'success';

/** Tipos de botón HTML estándar para retro-button y retro-icon-button. */
export type LibButtonType = 'button' | 'submit' | 'reset';

/** Tamaños del retro-button: sm (32px, denso), md (40px, compacto), lg (44px, default). */
export type RetroButtonSize = 'sm' | 'md' | 'lg';
