/** Variantes visuales del botón retro-button Terminal Collector. */
export type LibButtonVariant = 'primary' | 'ghost' | 'danger' | 'success';

/** Tipos de botón HTML estándar para retro-button. */
export type LibButtonType = 'button' | 'submit' | 'reset';

/** Variantes visuales de retro-card. */
export type LibCardVariant = 'default' | 'accent' | 'muted';

/** Colores semánticos disponibles para retro-chip. */
export type RetroChipColor = 'primary' | 'green' | 'amber' | 'rose' | 'blue' | 'neutral';

/** Variantes visuales del retro-badge Terminal Collector. */
export type RetroBadgeVariant = 'primary' | 'success' | 'warning' | 'danger' | 'info' | 'neutral';

/** Variantes visuales del retro-icon-button Terminal Collector. */
export type LibIconButtonVariant = 'primary' | 'ghost' | 'danger';

/** Tamaños del retro-icon-button: sm (32) matSuffix, md (40) topbars, lg (44) standalone. */
export type LibIconButtonSize = 'sm' | 'md' | 'lg';

/** Tamaños del retro-icon: xs (chip/data-row), sm (botón/tabs), md (menús), lg (headers), xl (topbar), 2xl (empty states). */
export type LibIconSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl';

/** Tamaños del retro-spinner Terminal Collector. */
export type LibSpinnerSize = 'inline' | 'sm' | 'md' | 'lg';

/** Variantes de glyph del retro-spinner Terminal Collector. */
export type LibSpinnerVariant = 'bars' | 'dots' | 'blocks';

/** Alias semántico de forma del retro-skeleton (no altera la geometría rectangular). */
export type LibSkeletonShape = 'line' | 'square' | 'block';

/** Tamaños del retro-checkbox: sm (0.875rem) o md (1rem, default). */
export type LibCheckboxSize = 'sm' | 'md';

/** Variantes de color del retro-snackbar Terminal Collector. */
export type LibSnackbarVariant = 'info' | 'success' | 'warning' | 'error';

/** Posiciones del retro-tooltip. */
export type LibTooltipPosition = 'top' | 'bottom' | 'left' | 'right';
